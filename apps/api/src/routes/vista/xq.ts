// Auto-generated by vista-module-gen for XQ (XQ)
// Do NOT edit manually -- regenerate with: node tools/vista-module-gen/cli.mjs --package XQ
import type { FastifyInstance } from 'fastify';
import { requireSession } from '../../auth/auth-routes.js';
import { safeCallRpc } from '../../lib/rpc-resilience.js';

export async function xqRoutes(server: FastifyInstance) {

  /** RPC: XQAL GUI ALERTS (GLOBAL ARRAY) — 1 param(s) */
  server.get('/vista/xq/rpc/xqal-gui-alerts', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('XQAL GUI ALERTS', [q.data || '']);
    return { ok: true, source: 'vista', rpcUsed: ['XQAL GUI ALERTS'], data: result };
  });

  /** RPC: XU REBUILD MENU TREE (SINGLE VALUE) — 0 param(s) */
  server.get('/vista/xq/rpc/xu-rebuild-menu-tree', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const result = await safeCallRpc('XU REBUILD MENU TREE', []);
    return { ok: true, source: 'vista', rpcUsed: ['XU REBUILD MENU TREE'], data: result };
  });
}