// Auto-generated by vista-module-gen for IFCAP (PRC)
// Do NOT edit manually -- regenerate with: node tools/vista-module-gen/cli.mjs --package PRC
import type { FastifyInstance } from 'fastify';
import { requireSession } from '../../auth/auth-routes.js';
import { safeCallRpc } from '../../lib/rpc-resilience.js';

export async function prcRoutes(server: FastifyInstance) {

  /** RPC: PRCHL LIST (GLOBAL ARRAY) — 2 param(s) */
  server.get('/vista/prc/rpc/prchl-list', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('PRCHL LIST', [q.date1 || '', q.date2 || '']);
    return { ok: true, source: 'vista', rpcUsed: ['PRCHL LIST'], data: result };
  });

  /** RPC: PRCHL LIST X (GLOBAL ARRAY) — 0 param(s) */
  server.get('/vista/prc/rpc/prchl-list-x', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const result = await safeCallRpc('PRCHL LIST X', []);
    return { ok: true, source: 'vista', rpcUsed: ['PRCHL LIST X'], data: result };
  });

  /** RPC: PRCHL ITEM DET (ARRAY) — 1 param(s) */
  server.get('/vista/prc/rpc/prchl-item-det', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('PRCHL ITEM DET', [q.ien || '']);
    return { ok: true, source: 'vista', rpcUsed: ['PRCHL ITEM DET'], data: result };
  });
}