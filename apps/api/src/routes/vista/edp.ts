// Auto-generated by vista-module-gen for Emergency Department Integration Software (EDP)
// Do NOT edit manually -- regenerate with: node tools/vista-module-gen/cli.mjs --package EDP
import type { FastifyInstance } from 'fastify';
import { requireSession } from '../../auth/auth-routes.js';
import { safeCallRpc } from '../../lib/rpc-resilience.js';

export async function edpRoutes(server: FastifyInstance) {

  /** RPC: EDPCTRL RPC (ARRAY) — 1 param(s) */
  server.get('/vista/edp/rpc/edpctrl-rpc', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('EDPCTRL RPC', [q.params || '']);
    return { ok: true, source: 'vista', rpcUsed: ['EDPCTRL RPC'], data: result };
  });

  /** RPC: EDPCBRD RPC (ARRAY) — 2 param(s) */
  server.get('/vista/edp/rpc/edpcbrd-rpc', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('EDPCBRD RPC', [q.sess || '', q.params || '']);
    return { ok: true, source: 'vista', rpcUsed: ['EDPCBRD RPC'], data: result };
  });

  /** RPC: EDPGLOB RPC (GLOBAL ARRAY) — 2 param(s) */
  server.get('/vista/edp/rpc/edpglob-rpc', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('EDPGLOB RPC', [q.params || '', q.params2 || '']);
    return { ok: true, source: 'vista', rpcUsed: ['EDPGLOB RPC'], data: result };
  });
}