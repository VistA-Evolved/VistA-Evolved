// Auto-generated by vista-module-gen for Enrollment Application System (EAS)
// Do NOT edit manually -- regenerate with: node tools/vista-module-gen/cli.mjs --package EAS
import type { FastifyInstance } from 'fastify';
import { requireSession } from '../../auth/auth-routes.js';
import { safeCallRpc } from '../../lib/rpc-resilience.js';

export async function easRoutes(server: FastifyInstance) {

  /** RPC: EAS ESR MESSAGING (ARRAY) — 1 param(s) */
  server.get('/vista/eas/rpc/eas-esr-messaging', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('EAS ESR MESSAGING', [q.mode || '']);
    return { ok: true, source: 'vista', rpcUsed: ['EAS ESR MESSAGING'], data: result };
  });
}