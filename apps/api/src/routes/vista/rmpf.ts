// Auto-generated by vista-module-gen for Remote Order Entry System (RMPF)
// Do NOT edit manually -- regenerate with: node tools/vista-module-gen/cli.mjs --package RMPF
import type { FastifyInstance } from 'fastify';
import { requireSession } from '../../auth/auth-routes.js';
import { safeCallRpc } from '../../lib/rpc-resilience.js';

export async function rmpfRoutes(server: FastifyInstance) {

  /** RPC: RMPFDEMOG (ARRAY) — 1 param(s) */
  server.get('/vista/rmpf/rpc/rmpfdemog', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    if (!q.dfn) return { ok: false, error: 'dfn query parameter is required' };
    const result = await safeCallRpc('RMPFDEMOG', [q.dfn]);
    return { ok: true, source: 'vista', rpcUsed: ['RMPFDEMOG'], data: result };
  });
}