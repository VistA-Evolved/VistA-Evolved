// Auto-generated by vista-module-gen for E Claims Management Engine (BPS)
// Do NOT edit manually -- regenerate with: node tools/vista-module-gen/cli.mjs --package BPS
import type { FastifyInstance } from 'fastify';
import { requireSession } from '../../auth/auth-routes.js';
import { safeCallRpc } from '../../lib/rpc-resilience.js';

export async function bpsRoutes(server: FastifyInstance) {

  /** RPC: BPS TAS CLAIM DATA EXTRACT (GLOBAL ARRAY) — 1 param(s) */
  server.get('/vista/bps/rpc/bps-tas-claim-data-extract', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('BPS TAS CLAIM DATA EXTRACT', [q.bps59 || '']);
    return { ok: true, source: 'vista', rpcUsed: ['BPS TAS CLAIM DATA EXTRACT'], data: result };
  });

  /** RPC: BPS TAS TXN IENS NEW (GLOBAL ARRAY) — 2 param(s) */
  server.get('/vista/bps/rpc/bps-tas-txn-iens-new', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('BPS TAS TXN IENS NEW', [q.count || '', q.log || '']);
    return { ok: true, source: 'vista', rpcUsed: ['BPS TAS TXN IENS NEW'], data: result };
  });

  /** RPC: BPS TAS CLAIM WRITE BACK (ARRAY) — 1 param(s) */
  server.get('/vista/bps/rpc/bps-tas-claim-write-back', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('BPS TAS CLAIM WRITE BACK', [q.ien || '']);
    return { ok: true, source: 'vista', rpcUsed: ['BPS TAS CLAIM WRITE BACK'], data: result };
  });

  /** RPC: BPS TAS DATABASE RESET (ARRAY) — 2 param(s) */
  server.get('/vista/bps/rpc/bps-tas-database-reset', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('BPS TAS DATABASE RESET', [q.flag || '', q.date || '']);
    return { ok: true, source: 'vista', rpcUsed: ['BPS TAS DATABASE RESET'], data: result };
  });
}