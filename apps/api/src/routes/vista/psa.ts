// Auto-generated by vista-module-gen for Drug Accountability (PSA)
// Do NOT edit manually -- regenerate with: node tools/vista-module-gen/cli.mjs --package PSA
import type { FastifyInstance } from 'fastify';
import { requireSession } from '../../auth/auth-routes.js';
import { safeCallRpc } from '../../lib/rpc-resilience.js';

export async function psaRoutes(server: FastifyInstance) {

  /** RPC: PSA UPLOAD (ARRAY) — 1 param(s) */
  server.get('/vista/psa/rpc/psa-upload', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('PSA UPLOAD', [q.tmp || '']);
    return { ok: true, source: 'vista', rpcUsed: ['PSA UPLOAD'], data: result };
  });
}