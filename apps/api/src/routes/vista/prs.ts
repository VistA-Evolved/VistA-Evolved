// Auto-generated by vista-module-gen for PAID (PRS)
// Do NOT edit manually -- regenerate with: node tools/vista-module-gen/cli.mjs --package PRS
import type { FastifyInstance } from 'fastify';
import { requireSession } from '../../auth/auth-routes.js';
import { safeCallRpc } from '../../lib/rpc-resilience.js';

export async function prsRoutes(server: FastifyInstance) {

  /** RPC: PRSN NURS LOCATION EXTRACTION (GLOBAL ARRAY) — 1 param(s) */
  server.get('/vista/prs/rpc/prsn-nurs-location-extraction', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('PRSN NURS LOCATION EXTRACTION', [q.prsndt || '']);
    return { ok: true, source: 'vista', rpcUsed: ['PRSN NURS LOCATION EXTRACTION'], data: result };
  });

  /** RPC: PRSN POC DAILY TIME EXTRACTION (GLOBAL ARRAY) — 2 param(s) */
  server.get('/vista/prs/rpc/prsn-poc-daily-time-extraction', async (request, reply) => {
    const session = await requireSession(request, reply);
    if (!session) return;
    const q = (request.query || request.body || {}) as Record<string, string>;
    const result = await safeCallRpc('PRSN POC DAILY TIME EXTRACTION', [q.prsnseq || '', q.prsnrec || '']);
    return { ok: true, source: 'vista', rpcUsed: ['PRSN POC DAILY TIME EXTRACTION'], data: result };
  });
}