// Auto-generated by vista-module-gen for Patient Assessment Documentation (NUPA)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"NUPA PAST/FUTURE DATE","slug":"nupa-pastfuture-date"},{"name":"NUPA GET CAREPLAN DA","slug":"nupa-get-careplan-da"},{"name":"NUPA CAREPLAN PROBS","slug":"nupa-careplan-probs"},{"name":"NUPA SET PU AND ALT","slug":"nupa-set-pu-and-alt"},{"name":"NUPA WP GET","slug":"nupa-wp-get"},{"name":"NUPA WP SET","slug":"nupa-wp-set"},{"name":"NUPA RUN OBJECT (>1 LINE)","slug":"nupa-run-object-1-line"},{"name":"NUPA USER CLASS","slug":"nupa-user-class"},{"name":"NUPA SCREEN","slug":"nupa-screen"},{"name":"NUPA DLOOK","slug":"nupa-dlook"},{"name":"NUPA LIST","slug":"nupa-list"},{"name":"NUPA LIST2","slug":"nupa-list2"},{"name":"NUPA REMINDERS COLLECT","slug":"nupa-reminders-collect"},{"name":"NUPA REMINDERS GET","slug":"nupa-reminders-get"},{"name":"NUPA REMINDERS MANUAL","slug":"nupa-reminders-manual"},{"name":"NUPA FILE","slug":"nupa-file"},{"name":"NUPA LOOKUP","slug":"nupa-lookup"},{"name":"NUPA NEW","slug":"nupa-new"},{"name":"NUPA NEW IF NONE","slug":"nupa-new-if-none"},{"name":"NUPA CAREPLAN PROBLEM HISTORY","slug":"nupa-careplan-problem-history"},{"name":"NUPA PRINT","slug":"nupa-print"},{"name":"NUPA ORDERS","slug":"nupa-orders"},{"name":"NUPA INP MEDS LIST","slug":"nupa-inp-meds-list"},{"name":"NUPA CAREPLAN HISTORY","slug":"nupa-careplan-history"},{"name":"NUPA LOOKUP TRY 2","slug":"nupa-lookup-try-2"},{"name":"NUPA DP COMMENT HISTORY","slug":"nupa-dp-comment-history"},{"name":"NUPA GET PRESSURE ULCERS","slug":"nupa-get-pressure-ulcers"},{"name":"NUPA GET IVS","slug":"nupa-get-ivs"},{"name":"NUPA GET COMPONENTS","slug":"nupa-get-components"},{"name":"NUPA GET SKIN ALT","slug":"nupa-get-skin-alt"},{"name":"NUPA GET GI DEVICES","slug":"nupa-get-gi-devices"},{"name":"NUPA REASSESSMENT RADIOBUTTONS","slug":"nupa-reassessment-radiobuttons"},{"name":"NUPA GET HEALTH FACTORS","slug":"nupa-get-health-factors"},{"name":"NUPA GET ALLERGIES","slug":"nupa-get-allergies"},{"name":"NUPA GET APPTS","slug":"nupa-get-appts"},{"name":"NUPA AUDITC NOTE TEXT","slug":"nupa-auditc-note-text"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function PatientAssessmentDocumentationPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/nupa/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">36 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for NUPA.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/nupa/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}