// Auto-generated by vista-module-gen for Clinical Case Registries (ROR)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"ROR GET M VERSION","slug":"ror-get-m-version"},{"name":"ROR LOG MESSAGE DETAILS","slug":"ror-log-message-details"},{"name":"ROR REPORT RETRIEVE","slug":"ror-report-retrieve"},{"name":"ROR REPORT SCHEDULE","slug":"ror-report-schedule"},{"name":"ROR TASK INFO","slug":"ror-task-info"},{"name":"ROR TASK LIST","slug":"ror-task-list"},{"name":"ROR GUI ACCESS","slug":"ror-gui-access"},{"name":"ROR TASK DELETE","slug":"ror-task-delete"},{"name":"ROR LOG GET LIST","slug":"ror-log-get-list"},{"name":"ROR LOG GET MESSAGES","slug":"ror-log-get-messages"},{"name":"ROR GUI PARAMETER GET","slug":"ror-gui-parameter-get"},{"name":"ROR GUI PARAMETER SET","slug":"ror-gui-parameter-set"},{"name":"ROR LOG GET ACCESS VIOLATIONS","slug":"ror-log-get-access-violations"},{"name":"ROR LIST REGISTRY USERS","slug":"ror-list-registry-users"},{"name":"ROR GET REGISTRY INFO","slug":"ror-get-registry-info"},{"name":"ROR REPORTS AVAILABLE","slug":"ror-reports-available"},{"name":"ROR LIST DIVISIONS","slug":"ror-list-divisions"},{"name":"ROR LIST HOSPITAL LOCATIONS","slug":"ror-list-hospital-locations"},{"name":"ROR LIST VA DRUG CLASSES","slug":"ror-list-va-drug-classes"},{"name":"ROR LIST LABORATORY TESTS","slug":"ror-list-laboratory-tests"},{"name":"ROR LOG INFO","slug":"ror-log-info"},{"name":"ROR REPORT STYLESHEET","slug":"ror-report-stylesheet"},{"name":"ROR TASK VALIDATE RESCHEDULING","slug":"ror-task-validate-rescheduling"},{"name":"ROR LIST PATIENTS","slug":"ror-list-patients"},{"name":"ROR PATIENT SELECTION RULES","slug":"ror-patient-selection-rules"},{"name":"ROR LIST ITEMS","slug":"ror-list-items"},{"name":"ROR LIST COORDINATORS","slug":"ror-list-coordinators"},{"name":"ROR UPDATE COORDINATORS","slug":"ror-update-coordinators"},{"name":"ROR LIST VISTA USERS","slug":"ror-list-vista-users"},{"name":"RORICR CDC LOAD","slug":"roricr-cdc-load"},{"name":"ROR LIST STATES","slug":"ror-list-states"},{"name":"RORICR CDC SAVE","slug":"roricr-cdc-save"},{"name":"ROR GET USER INFO","slug":"ror-get-user-info"},{"name":"ROR PATIENT GET DATA","slug":"ror-patient-get-data"},{"name":"ROR PATIENT DELETE","slug":"ror-patient-delete"},{"name":"ROR LIST LOCAL LAB TESTS","slug":"ror-list-local-lab-tests"},{"name":"ROR UPDATE LOCAL LAB TESTS","slug":"ror-update-local-lab-tests"},{"name":"ROR LIST LOCAL DRUGS","slug":"ror-list-local-drugs"},{"name":"ROR UPDATE LOCAL DRUGS","slug":"ror-update-local-drugs"},{"name":"ROR LIST DRUGS","slug":"ror-list-drugs"},{"name":"RORICR PATIENT LOAD","slug":"roricr-patient-load"},{"name":"RORICR PATIENT SAVE","slug":"roricr-patient-save"},{"name":"RORICR CDC CANCEL","slug":"roricr-cdc-cancel"},{"name":"ROR LIST GENERIC DRUGS","slug":"ror-list-generic-drugs"},{"name":"ROR GET SERVER TIME","slug":"ror-get-server-time"},{"name":"RORHEPC PATIENT LOAD","slug":"rorhepc-patient-load"},{"name":"RORHEPC PATIENT SAVE","slug":"rorhepc-patient-save"},{"name":"ROR LIST PARAMETER INSTANCES","slug":"ror-list-parameter-instances"},{"name":"ROR GUI PARAMETER RENAME","slug":"ror-gui-parameter-rename"},{"name":"ROR LIST LOCAL FIELDS","slug":"ror-list-local-fields"},{"name":"ROR UPDATE LOCAL FIELDS","slug":"ror-update-local-fields"},{"name":"ROR LIST SELECTION RULES","slug":"ror-list-selection-rules"},{"name":"ROR PRODUCTION ACCOUNT","slug":"ror-production-account"},{"name":"ROR PATIENT REGISTRY LABS","slug":"ror-patient-registry-labs"},{"name":"ROR LIST CPT","slug":"ror-list-cpt"},{"name":"ROR LIST REGISTRY DRUGS","slug":"ror-list-registry-drugs"},{"name":"ROR LIST INVESTIGATIONAL DRUGS","slug":"ror-list-investigational-drugs"},{"name":"ROR LIST ICD","slug":"ror-list-icd"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function ClinicalCaseRegistriesPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/ror/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">58 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for ROR.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/ror/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}