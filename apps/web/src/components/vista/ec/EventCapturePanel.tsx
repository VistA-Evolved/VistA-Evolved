// Auto-generated by vista-module-gen for Event Capture (EC)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"EC GETDSSUNITUSRS","slug":"ec-getdssunitusrs"},{"name":"EC GETECSCREEN","slug":"ec-getecscreen"},{"name":"EC GETPXREASON","slug":"ec-getpxreason"},{"name":"EC GETECSDETAIL","slug":"ec-getecsdetail"},{"name":"EC GETPATPROCS","slug":"ec-getpatprocs"},{"name":"EC GETSCNHELP","slug":"ec-getscnhelp"},{"name":"EC GETECLOC","slug":"ec-getecloc"},{"name":"EC GETUSRDSSUNIT","slug":"ec-getusrdssunit"},{"name":"EC GETCPTLST","slug":"ec-getcptlst"},{"name":"EC GETECSPROCS","slug":"ec-getecsprocs"},{"name":"EC GETECSCATS","slug":"ec-getecscats"},{"name":"EC GETBATPROCS","slug":"ec-getbatprocs"},{"name":"EC REPORTS","slug":"ec-reports"},{"name":"EC GETIEN","slug":"ec-getien"},{"name":"EC GETPATINFO","slug":"ec-getpatinfo"},{"name":"EC GETPXMODIFIER","slug":"ec-getpxmodifier"},{"name":"EC GETPROVIDER","slug":"ec-getprovider"},{"name":"EC GETNATPX","slug":"ec-getnatpx"},{"name":"EC VALIDATE SPREADSHEET DATA","slug":"ec-validate-spreadsheet-data"},{"name":"EC GETPATELIG","slug":"ec-getpatelig"},{"name":"EC GETPATCLASTAT","slug":"ec-getpatclastat"},{"name":"EC GETENCDXS","slug":"ec-getencdxs"},{"name":"EC GETDSSUNIT","slug":"ec-getdssunit"},{"name":"EC GETCAT","slug":"ec-getcat"},{"name":"EC GETPXLST","slug":"ec-getpxlst"},{"name":"EC DSSCATCHECK","slug":"ec-dsscatcheck"},{"name":"EC FILER","slug":"ec-filer"},{"name":"EC GETLIST","slug":"ec-getlist"},{"name":"EC GETDSSECS","slug":"ec-getdssecs"},{"name":"EC GETLOC","slug":"ec-getloc"},{"name":"EC GETPRODEFS","slug":"ec-getprodefs"},{"name":"EC GETDATE","slug":"ec-getdate"},{"name":"EC CLASHELP","slug":"ec-clashelp"},{"name":"EC SPACEBAR","slug":"ec-spacebar"},{"name":"EC DIEDON","slug":"ec-diedon"},{"name":"EC GETVISITINFO","slug":"ec-getvisitinfo"},{"name":"EC GETPATCH","slug":"ec-getpatch"},{"name":"EC GETVERSION","slug":"ec-getversion"},{"name":"ECOB FACTORY","slug":"ecob-factory"},{"name":"ECOB METHOD","slug":"ecob-method"},{"name":"EC RECENT VISITS","slug":"ec-recent-visits"},{"name":"EC ICD10IMPLEMENTATIONDATE","slug":"ec-icd10implementationdate"},{"name":"EC GET DEFAULT PROVIDER","slug":"ec-get-default-provider"},{"name":"EC DELETE TEST PATIENT DATA","slug":"ec-delete-test-patient-data"},{"name":"EC GETPRBLST","slug":"ec-getprblst"},{"name":"EC DELETE FILE ENTRY","slug":"ec-delete-file-entry"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function EventCapturePanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/ec/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">46 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for EC.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/ec/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}