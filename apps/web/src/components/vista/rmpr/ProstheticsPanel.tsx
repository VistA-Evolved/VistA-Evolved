// Auto-generated by vista-module-gen for Prosthetics (RMPR)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"RMPR NPPD LIST","slug":"rmpr-nppd-list"},{"name":"RMPR TRANS LIST","slug":"rmpr-trans-list"},{"name":"RMPR PT DEM","slug":"rmpr-pt-dem"},{"name":"RMPR PT ADD","slug":"rmpr-pt-add"},{"name":"RMPR TRANS HISA","slug":"rmpr-trans-hisa"},{"name":"RMPR TRANS HOME O2","slug":"rmpr-trans-home-o2"},{"name":"RMPR CRITICAL COM","slug":"rmpr-critical-com"},{"name":"RMPR DIS LIST","slug":"rmpr-dis-list"},{"name":"RMPR ADD ITEM","slug":"rmpr-add-item"},{"name":"RMPR LAST PT MOVEMENTS","slug":"rmpr-last-pt-movements"},{"name":"RMPR VEN ADDRESS","slug":"rmpr-ven-address"},{"name":"RMPR CLINIC ENROLLMENT","slug":"rmpr-clinic-enrollment"},{"name":"RMPR CPRS DISPLAY","slug":"rmpr-cprs-display"},{"name":"RMPR CLINIC LETTER LIST","slug":"rmpr-clinic-letter-list"},{"name":"RMPR PENDING APPOINTMENTS","slug":"rmpr-pending-appointments"},{"name":"RMPR IFCAP CHECK AUTH","slug":"rmpr-ifcap-check-auth"},{"name":"RMPR CLINIC LETTER DISPLAY","slug":"rmpr-clinic-letter-display"},{"name":"RMPR TRANS LIST EXPANDED","slug":"rmpr-trans-list-expanded"},{"name":"RMPR TRANS HOME O2 RX","slug":"rmpr-trans-home-o2-rx"},{"name":"RMPR VIEW REQ","slug":"rmpr-view-req"},{"name":"RMPR VIEW REQ DESC","slug":"rmpr-view-req-desc"},{"name":"RMPR VIEW REQ IA NOTE","slug":"rmpr-view-req-ia-note"},{"name":"RMPR VIEW REQ COMP NOTE","slug":"rmpr-view-req-comp-note"},{"name":"RMPR DOR","slug":"rmpr-dor"},{"name":"RMPR PFFS LIST","slug":"rmpr-pffs-list"},{"name":"RMPR DIS LIST 660","slug":"rmpr-dis-list-660"},{"name":"RMPR PFFS INS","slug":"rmpr-pffs-ins"},{"name":"RMPR OPEN PEN LIST","slug":"rmpr-open-pen-list"},{"name":"RMPR LIST PCARD","slug":"rmpr-list-pcard"},{"name":"RMPR CREATE PO","slug":"rmpr-create-po"},{"name":"RMPR LIST COST CENTER","slug":"rmpr-list-cost-center"},{"name":"RMPR LIST BOC","slug":"rmpr-list-boc"},{"name":"RMPR CHECK PA AUTHORIZATION","slug":"rmpr-check-pa-authorization"},{"name":"RMPR SUSPENSE ACTIONS","slug":"rmpr-suspense-actions"},{"name":"RMPR SEND PURCHASE ORDER","slug":"rmpr-send-purchase-order"},{"name":"RMPR PO CREATE VISTA IEN","slug":"rmpr-po-create-vista-ien"},{"name":"RMPR ENCRYPT PCARD","slug":"rmpr-encrypt-pcard"},{"name":"RMPR MANAGER COMMENT","slug":"rmpr-manager-comment"},{"name":"RMPR DELETE PO","slug":"rmpr-delete-po"},{"name":"RMPR NPPD LIST X","slug":"rmpr-nppd-list-x"},{"name":"RMPR CHECK FUND CONTROL POINT","slug":"rmpr-check-fund-control-point"},{"name":"RMPR PC PO PRINT","slug":"rmpr-pc-po-print"},{"name":"RMPR CHECK PROS PATIENT","slug":"rmpr-check-pros-patient"},{"name":"RMPR CREATE WORK ORDER","slug":"rmpr-create-work-order"},{"name":"RMPR OWL SUSPENSE ACTIONS","slug":"rmpr-owl-suspense-actions"},{"name":"RMPR OWL WORK ORDER LIST","slug":"rmpr-owl-work-order-list"},{"name":"RMPR OWL PC PRINT","slug":"rmpr-owl-pc-print"},{"name":"RMPR OWL ITEM LIST","slug":"rmpr-owl-item-list"},{"name":"RMPR PURCHASE ORDER CONTROL","slug":"rmpr-purchase-order-control"},{"name":"RMPR OWL ADD ON LIST","slug":"rmpr-owl-add-on-list"},{"name":"RMPR OWL ENTER/EDIT ADD ON","slug":"rmpr-owl-enteredit-add-on"},{"name":"RMPR OWL ENTER/EDIT HCPCS CODE","slug":"rmpr-owl-enteredit-hcpcs-code"},{"name":"RMPR OWL ENTER/EDIT HRS/RATE","slug":"rmpr-owl-enteredit-hrsrate"},{"name":"RMPR OWL TECH/HOUR/RATE LIST","slug":"rmpr-owl-techhourrate-list"},{"name":"RMPR OWL VISTA PRINT","slug":"rmpr-owl-vista-print"},{"name":"RMPR OWL LOCK FILE","slug":"rmpr-owl-lock-file"},{"name":"RMPR SUSPENSE LOCK","slug":"rmpr-suspense-lock"},{"name":"RMPR SUSPENSE UNLOCK","slug":"rmpr-suspense-unlock"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function ProstheticsPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/rmpr/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">58 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for RMPR.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/rmpr/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}