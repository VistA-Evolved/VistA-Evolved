// Auto-generated by vista-module-gen for Kernel Security (XUS)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"XUS SIGNON SETUP","slug":"xus-signon-setup"},{"name":"XUS SEND KEYS","slug":"xus-send-keys"},{"name":"XUS AV CODE","slug":"xus-av-code"},{"name":"XUS INTRO MSG","slug":"xus-intro-msg"},{"name":"XUS KEY CHECK","slug":"xus-key-check"},{"name":"XUS CVC","slug":"xus-cvc"},{"name":"XUS AV HELP","slug":"xus-av-help"},{"name":"XUS DIVISION SET","slug":"xus-division-set"},{"name":"XUS GET USER INFO","slug":"xus-get-user-info"},{"name":"XUS DIVISION GET","slug":"xus-division-get"},{"name":"XUS KAAJEE GET USER VIA PROXY","slug":"xus-kaajee-get-user-via-proxy"},{"name":"XUS KAAJEE GET CCOW TOKEN","slug":"xus-kaajee-get-ccow-token"},{"name":"XUS GET TOKEN","slug":"xus-get-token"},{"name":"XUS CCOW VAULT PARAM","slug":"xus-ccow-vault-param"},{"name":"XUS GET CCOW TOKEN","slug":"xus-get-ccow-token"},{"name":"XUS ALLKEYS","slug":"xus-allkeys"},{"name":"XUS SET VISITOR","slug":"xus-set-visitor"},{"name":"XUS GET VISITOR","slug":"xus-get-visitor"},{"name":"XUS KAAJEE GET USER INFO","slug":"xus-kaajee-get-user-info"},{"name":"XUS KAAJEE LOGOUT","slug":"xus-kaajee-logout"},{"name":"XUS PKI GET UPN","slug":"xus-pki-get-upn"},{"name":"XUS PKI SET UPN","slug":"xus-pki-set-upn"},{"name":"XUS IAM ADD USER","slug":"xus-iam-add-user"},{"name":"XUS IAM EDIT USER","slug":"xus-iam-edit-user"},{"name":"XUS IAM FIND USER","slug":"xus-iam-find-user"},{"name":"XUS IAM DISPLAY USER","slug":"xus-iam-display-user"},{"name":"XUS ESSO VALIDATE","slug":"xus-esso-validate"},{"name":"XUS IAM BIND USER","slug":"xus-iam-bind-user"},{"name":"XUS IAM TERMINATE USER","slug":"xus-iam-terminate-user"},{"name":"XUS IAM REACTIVATE USER","slug":"xus-iam-reactivate-user"},{"name":"XUS BSE TOKEN","slug":"xus-bse-token"},{"name":"XUS MVI NEW PERSON GET","slug":"xus-mvi-new-person-get"},{"name":"XUS MVI NEW PERSON UPDATE","slug":"xus-mvi-new-person-update"},{"name":"XUS MVI NEW PERSON DATA","slug":"xus-mvi-new-person-data"},{"name":"XUS MVI ENRICH NEW PERSON","slug":"xus-mvi-enrich-new-person"},{"name":"XUS MVI NEW PERSON BULK GET","slug":"xus-mvi-new-person-bulk-get"},{"name":"XUS IS USER ACTIVE","slug":"xus-is-user-active"},{"name":"XUS MVI NEW PERSON RMTE AUDIT","slug":"xus-mvi-new-person-rmte-audit"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function KernelSecurityPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/xus/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">38 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for XUS.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/xus/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}