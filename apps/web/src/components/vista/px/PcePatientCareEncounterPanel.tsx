// Auto-generated by vista-module-gen for PCE Patient Care Encounter (PX)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"PXRM REMINDER CATEGORY","slug":"pxrm-reminder-category"},{"name":"PXRM REMINDERS AND CATEGORIES","slug":"pxrm-reminders-and-categories"},{"name":"ORQQPX GET NOT PURPOSE","slug":"orqqpx-get-not-purpose"},{"name":"ORQQPXRM GET WH PROC RESULT","slug":"orqqpxrm-get-wh-proc-result"},{"name":"PXRM EDUCATION SUBTOPICS","slug":"pxrm-education-subtopics"},{"name":"PXRM EDUCATION SUMMARY","slug":"pxrm-education-summary"},{"name":"PXRM EDUCATION TOPIC","slug":"pxrm-education-topic"},{"name":"PXRM REMINDER CATEGORIES","slug":"pxrm-reminder-categories"},{"name":"PXRM REMINDER DIALOG","slug":"pxrm-reminder-dialog"},{"name":"PXRM REMINDER DIALOG PROMPTS","slug":"pxrm-reminder-dialog-prompts"},{"name":"PXRM REMINDER EVALUATION","slug":"pxrm-reminder-evaluation"},{"name":"PXRM REMINDER INQUIRY","slug":"pxrm-reminder-inquiry"},{"name":"PXRM REMINDER WEB","slug":"pxrm-reminder-web"},{"name":"PXRM REMINDERS (UNEVALUATED)","slug":"pxrm-reminders-unevaluated"},{"name":"PXRM REMINDER DETAIL","slug":"pxrm-reminder-detail"},{"name":"PXRM MENTAL HEALTH","slug":"pxrm-mental-health"},{"name":"PXRM PROGRESS NOTE HEADER","slug":"pxrm-progress-note-header"},{"name":"PXRM MENTAL HEALTH RESULTS","slug":"pxrm-mental-health-results"},{"name":"PXRM MENTAL HEALTH SAVE","slug":"pxrm-mental-health-save"},{"name":"PXRM REMINDER RPC","slug":"pxrm-reminder-rpc"},{"name":"PX SAVE DATA","slug":"px-save-data"},{"name":"PXVIMM INFO SOURCE","slug":"pxvimm-info-source"},{"name":"PXVIMM ADMIN ROUTE","slug":"pxvimm-admin-route"},{"name":"PXVIMM ADMIN SITE","slug":"pxvimm-admin-site"},{"name":"PXVIMM IMM LOT","slug":"pxvimm-imm-lot"},{"name":"PXVIMM IMM MAN","slug":"pxvimm-imm-man"},{"name":"PXVIMM VIS","slug":"pxvimm-vis"},{"name":"PXVIMM IMMDATA","slug":"pxvimm-immdata"},{"name":"PXVIMM ICR LIST","slug":"pxvimm-icr-list"},{"name":"PXVIMM VICR EVENTS","slug":"pxvimm-vicr-events"},{"name":"PXVIMM IMM SHORT LIST","slug":"pxvimm-imm-short-list"},{"name":"PXVIMM IMM DETAILED","slug":"pxvimm-imm-detailed"},{"name":"PXVIMM ADMIN CODES","slug":"pxvimm-admin-codes"},{"name":"PXVIMM IMM FORMAT","slug":"pxvimm-imm-format"},{"name":"PXVIMM VIMM DATA","slug":"pxvimm-vimm-data"},{"name":"PXVSK SKIN SHORT LIST","slug":"pxvsk-skin-short-list"},{"name":"PXVSK DEF SITES","slug":"pxvsk-def-sites"},{"name":"PXVSK V SKIN TEST LIST","slug":"pxvsk-v-skin-test-list"},{"name":"PXVIMM IMM DISCLOSURE","slug":"pxvimm-imm-disclosure"},{"name":"PXRMRPCG GENFUPD","slug":"pxrmrpcg-genfupd"},{"name":"PXRMRPCC PROMPTVL","slug":"pxrmrpcc-promptvl"},{"name":"PXRMRPCG CANCEL","slug":"pxrmrpcg-cancel"},{"name":"PXRMRPCG VIEW","slug":"pxrmrpcg-view"},{"name":"PXRMRPCG GENFVALD","slug":"pxrmrpcg-genfvald"},{"name":"PX ICE WEB","slug":"px-ice-web"},{"name":"PXRPC SAVE2","slug":"pxrpc-save2"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function PcePatientCareEncounterPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/px/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">46 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for PX.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/px/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}