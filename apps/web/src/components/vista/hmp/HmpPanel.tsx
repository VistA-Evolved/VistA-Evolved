// Auto-generated by vista-module-gen for HMP (HMP)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"HMP PATIENT ACTIVITY","slug":"hmp-patient-activity"},{"name":"HMP APPOINTMENTS","slug":"hmp-appointments"},{"name":"HMP DATA VERSION","slug":"hmp-data-version"},{"name":"HMP DELETE OBJECT","slug":"hmp-delete-object"},{"name":"HMP GET CHECKSUM","slug":"hmp-get-checksum"},{"name":"HMP GET OBJECT","slug":"hmp-get-object"},{"name":"HMP GET OPERATIONAL DATA","slug":"hmp-get-operational-data"},{"name":"HMP GET PATIENT DATA","slug":"hmp-get-patient-data"},{"name":"HMP GET PATIENT DATA JSON","slug":"hmp-get-patient-data-json"},{"name":"HMP GET REFERENCE DATA","slug":"hmp-get-reference-data"},{"name":"HMP INPATIENTS","slug":"hmp-inpatients"},{"name":"HMP PUT DEMOGRAPHICS","slug":"hmp-put-demographics"},{"name":"HMP PUT OBJECT","slug":"hmp-put-object"},{"name":"HMP PUT PATIENT DATA","slug":"hmp-put-patient-data"},{"name":"HMP SUBSCRIBE","slug":"hmp-subscribe"},{"name":"HMP WRITEBACK PT DEM","slug":"hmp-writeback-pt-dem"},{"name":"HMPCPRS RPC","slug":"hmpcprs-rpc"},{"name":"HMPCRPC RPC","slug":"hmpcrpc-rpc"},{"name":"HMPCRPC RPCCHAIN","slug":"hmpcrpc-rpcchain"},{"name":"HMPDJFS API","slug":"hmpdjfs-api"},{"name":"HMPDJFS DELSUB","slug":"hmpdjfs-delsub"},{"name":"HMPFPTC CHKS","slug":"hmpfptc-chks"},{"name":"HMPFPTC LOG","slug":"hmpfptc-log"},{"name":"HMP LOCAL CORRESPONDINGIDS","slug":"hmp-local-correspondingids"},{"name":"HMP PUT OPERATIONAL DATA","slug":"hmp-put-operational-data"},{"name":"HMP PATIENT SCHED SYNC","slug":"hmp-patient-sched-sync"},{"name":"HMP PATIENT ADMIT SYNC","slug":"hmp-patient-admit-sync"},{"name":"HMP CHKXTMP","slug":"hmp-chkxtmp"},{"name":"HMP GLOBAL SIZE","slug":"hmp-global-size"},{"name":"HMP DEFAULT PATIENT LIST","slug":"hmp-default-patient-list"},{"name":"HMP PATIENT SELECT","slug":"hmp-patient-select"},{"name":"HMP SUBSCRIPTION STATUS","slug":"hmp-subscription-status"},{"name":"HMP MED ORDER CHECKS","slug":"hmp-med-order-checks"},{"name":"HMP GET RELATED ORDERS","slug":"hmp-get-related-orders"},{"name":"HMP WRITEBACK VITAL","slug":"hmp-writeback-vital"},{"name":"HMP WRITEBACK VITAL EIE","slug":"hmp-writeback-vital-eie"},{"name":"HMP WRITEBACK ENCOUNTERS","slug":"hmp-writeback-encounters"},{"name":"HMP WRITEBACK ALLERGY","slug":"hmp-writeback-allergy"},{"name":"HMP WRITEBACK ALLERGY EIE","slug":"hmp-writeback-allergy-eie"},{"name":"HMP WRITEBACK SIGN ORDERS","slug":"hmp-writeback-sign-orders"},{"name":"HMP TIU LONG LIST OF TITLES","slug":"hmp-tiu-long-list-of-titles"},{"name":"HMP RESOURCE VALUES","slug":"hmp-resource-values"},{"name":"HMP RESOURCE SLOT CLEAR","slug":"hmp-resource-slot-clear"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function HmpPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/hmp/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">43 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for HMP.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/hmp/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}