// Auto-generated by vista-module-gen for RPC Broker (XWB)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"XWB EGCHO STRING","slug":"xwb-egcho-string"},{"name":"XWB EGCHO LIST","slug":"xwb-egcho-list"},{"name":"XWB EGCHO BIG LIST","slug":"xwb-egcho-big-list"},{"name":"XWB EGCHO SORT LIST","slug":"xwb-egcho-sort-list"},{"name":"XWB EGCHO MEMO","slug":"xwb-egcho-memo"},{"name":"XWB FILE LIST","slug":"xwb-file-list"},{"name":"XWB FILENAME CHECK","slug":"xwb-filename-check"},{"name":"XWB RPC LIST","slug":"xwb-rpc-list"},{"name":"XWB GET VARIABLE VALUE","slug":"xwb-get-variable-value"},{"name":"XWB CREATE CONTEXT","slug":"xwb-create-context"},{"name":"XWB EXAMPLE ECHO STRING","slug":"xwb-example-echo-string"},{"name":"XWB EXAMPLE GET LIST","slug":"xwb-example-get-list"},{"name":"XWB EXAMPLE WPTEXT","slug":"xwb-example-wptext"},{"name":"XWB EXAMPLE SORT NUMBERS","slug":"xwb-example-sort-numbers"},{"name":"XWB GET BROKER INFO","slug":"xwb-get-broker-info"},{"name":"XWB IM HERE","slug":"xwb-im-here"},{"name":"XWB IS RPC AVAILABLE","slug":"xwb-is-rpc-available"},{"name":"XWB ARE RPCS AVAILABLE","slug":"xwb-are-rpcs-available"},{"name":"XWB REMOTE RPC","slug":"xwb-remote-rpc"},{"name":"XWB REMOTE STATUS CHECK","slug":"xwb-remote-status-check"},{"name":"XWB REMOTE GETDATA","slug":"xwb-remote-getdata"},{"name":"XWB DEFERRED RPC","slug":"xwb-deferred-rpc"},{"name":"XWB DEFERRED STATUS","slug":"xwb-deferred-status"},{"name":"XWB DEFERRED GETDATA","slug":"xwb-deferred-getdata"},{"name":"XWB DIRECT RPC","slug":"xwb-direct-rpc"},{"name":"XWB REMOTE CLEAR","slug":"xwb-remote-clear"},{"name":"XWB DEFERRED CLEAR","slug":"xwb-deferred-clear"},{"name":"XWB DEFERRED CLEARALL","slug":"xwb-deferred-clearall"},{"name":"XWB EXAMPLE TRAP PARAMS","slug":"xwb-example-trap-params"},{"name":"XWB M2M EXAMPLE LARRY","slug":"xwb-m2m-example-larry"},{"name":"XWB M2M EXAMPLE REF","slug":"xwb-m2m-example-ref"},{"name":"XWB EXAMPLE GLOBAL SORT","slug":"xwb-example-global-sort"},{"name":"XWB EXAMPLE BIG TEXT","slug":"xwb-example-big-text"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function RpcBrokerPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/xwb/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">33 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for XWB.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/xwb/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}