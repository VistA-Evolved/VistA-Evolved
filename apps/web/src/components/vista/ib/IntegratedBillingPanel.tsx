// Auto-generated by vista-module-gen for Integrated Billing (IB)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"IBD EXPAND FORMID","slug":"ibd-expand-formid"},{"name":"IBD GET FORMSPEC","slug":"ibd-get-formspec"},{"name":"IBD GET INPUT OBJECT BY CLINIC","slug":"ibd-get-input-object-by-clinic"},{"name":"IBD GET INPUT OBJECT BY FORM","slug":"ibd-get-input-object-by-form"},{"name":"IBD GET ONE INPUT OBJECT","slug":"ibd-get-one-input-object"},{"name":"IBD RECEIVE FORM DATA","slug":"ibd-receive-form-data"},{"name":"IBD VALIDATE USER","slug":"ibd-validate-user"},{"name":"IBD RECEIVE DATA","slug":"ibd-receive-data"},{"name":"IBD RETURN IMAGE ID","slug":"ibd-return-image-id"},{"name":"IBD ELAPSED TIME","slug":"ibd-elapsed-time"},{"name":"IBD STORE WORKSTATION ERROR","slug":"ibd-store-workstation-error"},{"name":"IBD STORE IMAGE NAME","slug":"ibd-store-image-name"},{"name":"IBD GET ALL PCE DATA","slug":"ibd-get-all-pce-data"},{"name":"IBD GET PAST APPT LIST","slug":"ibd-get-past-appt-list"},{"name":"IBARXM QUERY ONLY","slug":"ibarxm-query-only"},{"name":"IBARXM TRANS DATA","slug":"ibarxm-trans-data"},{"name":"IBARXM TRANS BILL","slug":"ibarxm-trans-bill"},{"name":"IBARXM QUERY SUPPRESS USER","slug":"ibarxm-query-suppress-user"},{"name":"IBO MT LTC COPAY QUERY","slug":"ibo-mt-ltc-copay-query"},{"name":"IBCN INSURANCE QUERY","slug":"ibcn-insurance-query"},{"name":"IBCN INSURANCE QUERY TASK","slug":"ibcn-insurance-query-task"},{"name":"IBD GET FORMSPEC GLOBAL","slug":"ibd-get-formspec-global"},{"name":"IBD GET SCAN PAGE INFO","slug":"ibd-get-scan-page-info"},{"name":"IBTAS 837 CLAIM DATA","slug":"ibtas-837-claim-data"},{"name":"IBTAS 837 EDICLAIMS","slug":"ibtas-837-ediclaims"},{"name":"IBTAS 837 ACK","slug":"ibtas-837-ack"},{"name":"IBTAS HEALTH","slug":"ibtas-health"},{"name":"IBECEA COPAY SYNCH","slug":"ibecea-copay-synch"},{"name":"IBTAS FACILITY BY VISN","slug":"ibtas-facility-by-visn"},{"name":"IBTAS 277STAT","slug":"ibtas-277stat"},{"name":"IBTAS 837 FHIR ENABLE","slug":"ibtas-837-fhir-enable"},{"name":"IBTAS 837 TEST CLAIMS","slug":"ibtas-837-test-claims"},{"name":"IBTAS 837 CLAIM NUMBER","slug":"ibtas-837-claim-number"},{"name":"IBTAS PCR IENS","slug":"ibtas-pcr-iens"},{"name":"IBTAS PCR CLAIM DATA","slug":"ibtas-pcr-claim-data"},{"name":"IBTAS PCR WRITEBACK","slug":"ibtas-pcr-writeback"},{"name":"IBTASDB POST","slug":"ibtasdb-post"},{"name":"IBMH COPAY SYNCH","slug":"ibmh-copay-synch"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function IntegratedBillingPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/ib/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">38 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for IB.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/ib/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}