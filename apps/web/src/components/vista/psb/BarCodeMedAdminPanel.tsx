// Auto-generated by vista-module-gen for Bar Code Med Admin (PSB)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"PSB SCANPT","slug":"psb-scanpt"},{"name":"PSB INSTRUCTOR","slug":"psb-instructor"},{"name":"PSB USERLOAD","slug":"psb-userload"},{"name":"PSB USERSAVE","slug":"psb-usersave"},{"name":"PSB FMDATE","slug":"psb-fmdate"},{"name":"PSB SCANMED","slug":"psb-scanmed"},{"name":"PSB PARAMETER","slug":"psb-parameter"},{"name":"PSB TRANSACTION","slug":"psb-transaction"},{"name":"PSB VALIDATE ORDER","slug":"psb-validate-order"},{"name":"PSB SERVER CLOCK VARIANCE","slug":"psb-server-clock-variance"},{"name":"PSB MEDICATION HISTORY","slug":"psb-medication-history"},{"name":"PSB GETPRNS","slug":"psb-getprns"},{"name":"PSB REPORT","slug":"psb-report"},{"name":"PSB SUBMIT MISSING DOSE","slug":"psb-submit-missing-dose"},{"name":"PSB VALIDATE ESIG","slug":"psb-validate-esig"},{"name":"PSB LOCK","slug":"psb-lock"},{"name":"PSB CHECK SERVER","slug":"psb-check-server"},{"name":"PSB MAIL","slug":"psb-mail"},{"name":"PSB GETORDERTAB","slug":"psb-getordertab"},{"name":"PSB WARDLIST","slug":"psb-wardlist"},{"name":"PSB GETIVPAR","slug":"psb-getivpar"},{"name":"PSB DEVICE","slug":"psb-device"},{"name":"PSB PUTIVPAR","slug":"psb-putivpar"},{"name":"PSB IV ORDER HISTORY","slug":"psb-iv-order-history"},{"name":"PSB BAG DETAIL","slug":"psb-bag-detail"},{"name":"PSB ALLERGY","slug":"psb-allergy"},{"name":"PSB GETPROVIDER","slug":"psb-getprovider"},{"name":"PSB MOB DRUG LIST","slug":"psb-mob-drug-list"},{"name":"PSB CPRS ORDER","slug":"psb-cprs-order"},{"name":"PSB NURS WARDLIST","slug":"psb-nurs-wardlist"},{"name":"PSB MAXDAYS","slug":"psb-maxdays"},{"name":"PSB VERSION CHECK","slug":"psb-version-check"},{"name":"PSB CHECK IV","slug":"psb-check-iv"},{"name":"PSB VITALS","slug":"psb-vitals"},{"name":"PSB VITAL MEAS FILE","slug":"psb-vital-meas-file"},{"name":"PSB MED LOG LOOKUP","slug":"psb-med-log-lookup"},{"name":"PSB COVERSHEET1","slug":"psb-coversheet1"},{"name":"PSB UTL XSTATUS SRCH","slug":"psb-utl-xstatus-srch"},{"name":"PSB MAN SCAN FAILURE","slug":"psb-man-scan-failure"},{"name":"PSB GETINJECTIONSITE","slug":"psb-getinjectionsite"},{"name":"PSB WITNESS","slug":"psb-witness"},{"name":"PSB CLINICLIST","slug":"psb-cliniclist"},{"name":"PSB MEDS ON PATIENT","slug":"psb-meds-on-patient"},{"name":"PSB GETSETWP","slug":"psb-getsetwp"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function BarCodeMedAdminPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/psb/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">44 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for PSB.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/psb/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}