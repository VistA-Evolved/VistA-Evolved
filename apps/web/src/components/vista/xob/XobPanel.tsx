// Auto-generated by vista-module-gen for XOB (XOB)
// Do NOT edit manually
'use client';

import { useState, useCallback } from 'react';

const API_BASE = typeof window !== 'undefined'
  ? `${window.location.protocol}//${window.location.hostname}:3001`
  : 'http://127.0.0.1:3001';

const RPCS: { name: string; slug: string }[] = [{"name":"XOBV TEST STRING","slug":"xobv-test-string"},{"name":"XOBV TEST GLOBAL ARRAY","slug":"xobv-test-global-array"},{"name":"XOBV TEST GLOBAL NODE","slug":"xobv-test-global-node"},{"name":"XOBV TEST LOCAL ARRAY","slug":"xobv-test-local-array"},{"name":"XOBV TEST VAR LENGTH REC","slug":"xobv-test-var-length-rec"},{"name":"XOBV TEST WORD PROCESSING","slug":"xobv-test-word-processing"},{"name":"XOBV TEST PING","slug":"xobv-test-ping"},{"name":"XOBV TEST RPC LIST","slug":"xobv-test-rpc-list"},{"name":"XOBV TEST XML RESULTS","slug":"xobv-test-xml-results"},{"name":"XOBV TEST NOT IN CONTEXT","slug":"xobv-test-not-in-context"},{"name":"XOBV TEST MULTIPLE SUBSCRIPTS","slug":"xobv-test-multiple-subscripts"},{"name":"XOBV TEST MULT ARRAY PARAMS","slug":"xobv-test-mult-array-params"},{"name":"XOBV SYSTEMINFO","slug":"xobv-systeminfo"},{"name":"XOBE ESIG GET CODE","slug":"xobe-esig-get-code"},{"name":"XOBE ESIG SET CODE","slug":"xobe-esig-set-code"},{"name":"XOBE ESIG GET DATA","slug":"xobe-esig-get-data"},{"name":"XOBE ESIG SET DATA","slug":"xobe-esig-set-data"},{"name":"XOBE ESIG IS DEFINED","slug":"xobe-esig-is-defined"},{"name":"XOBV VALIDATE SAML","slug":"xobv-validate-saml"}];

interface RpcResult { ok: boolean; rpcUsed?: string[]; data?: string[]; error?: string; }

export default function XobPanel({ dfn: propDfn = '46' }: { dfn?: string }) {
  const [selectedRpc, setSelectedRpc] = useState<string | null>(null);
  const [dfn, setDfn] = useState(propDfn);
  const [result, setResult] = useState<RpcResult | null>(null);
  const [loading, setLoading] = useState(false);

  const callRpc = useCallback(async (slug: string) => {
    setLoading(true);
    setSelectedRpc(slug);
    try {
      const res = await fetch(`${API_BASE}/vista/xob/rpc/${slug}?dfn=${dfn}`, { credentials: 'include' });
      const json = await res.json();
      setResult(json);
    } catch (e: unknown) {
      setResult({ ok: false, error: (e as Error).message });
    }
    setLoading(false);
  }, [dfn]);

  return (
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <label className="text-sm font-medium">Patient DFN:</label>
        <input
          className="px-2 py-1 border rounded text-sm w-24"
          value={dfn}
          onChange={e => setDfn(e.target.value)}
        />
        <span className="text-xs text-muted-foreground">19 RPCs available</span>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="md:col-span-1 border rounded-lg p-3 max-h-96 overflow-y-auto">
          <h3 className="font-semibold text-sm mb-2">RPCs</h3>
          {RPCS.length === 0 ? (
            <p className="text-xs text-muted-foreground">No RPCs registered for XOB.</p>
          ) : (
            <div className="space-y-1">
              {RPCS.map(r => (
                <button
                  key={r.slug}
                  onClick={() => callRpc(r.slug)}
                  className={`w-full text-left text-xs px-2 py-1.5 rounded transition-colors ${
                    selectedRpc === r.slug ? 'bg-primary text-primary-foreground' : 'hover:bg-muted'
                  }`}
                >
                  {r.name}
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="md:col-span-2 border rounded-lg p-3">
          <h3 className="font-semibold text-sm mb-2">
            {selectedRpc ? `Result: ${selectedRpc}` : 'Select an RPC to call'}
          </h3>
          {loading && <div className="animate-pulse text-sm">Calling VistA...</div>}
          {result && !loading && (
            <div className="space-y-2">
              <div className="flex gap-2 text-xs">
                <span className={`px-1.5 py-0.5 rounded ${result.ok ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>
                  {result.ok ? 'OK' : 'ERROR'}
                </span>
                {result.rpcUsed?.map(r => (
                  <span key={r} className="px-1.5 py-0.5 rounded bg-blue-100 text-blue-800">{r}</span>
                ))}
              </div>
              {result.error && <p className="text-red-500 text-sm">{result.error}</p>}
              {result.data && result.data.length > 0 ? (
                <pre className="text-xs bg-muted p-2 rounded max-h-60 overflow-y-auto whitespace-pre-wrap">
                  {result.data.join('\n')}
                </pre>
              ) : (
                <p className="text-muted-foreground text-sm">No data returned.</p>
              )}
            </div>
          )}
          {!result && !loading && (
            <p className="text-muted-foreground text-sm">Click an RPC on the left to call it against VistA.</p>
          )}
        </div>
      </div>

      <div className="text-xs text-muted-foreground">
        API: <code>${API_BASE}/vista/xob/rpc/&lt;slug&gt;?dfn=&lt;n&gt;</code>
      </div>
    </div>
  );
}