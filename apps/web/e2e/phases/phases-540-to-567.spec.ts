/**
 * Auto-generated by Phase 108 -- Phase Audit Harness
 * Group: phases-540-to-567
 * Phases: 540, 541, 556, 567
 *
 * DO NOT EDIT MANUALLY -- regenerate with:
 *   node scripts/generate-phase-qa.mjs
 */

import { test, expect } from '@playwright/test';
import { readdirSync } from 'node:fs';
import { join, resolve } from 'node:path';

const API = process.env.API_URL ?? 'http://localhost:3001';
const ROOT = resolve(__dirname, '..', '..', '..', '..');

test.describe('Phase 540: JLV-style Longitudinal Viewer v1', () => {
  test('API routes respond (no 500)', async ({ request }) => {
    const r0 = await request.get(`${API}/vista/longitudinal/meds-summary`);
    expect(r0.status(), '/vista/longitudinal/meds-summary not 500').not.toBe(500);
    const r1 = await request.get(`${API}/vista/longitudinal/meds-summary?dfn=N`);
    expect(r1.status(), '/vista/longitudinal/meds-summary?dfn=N not 500').not.toBe(500);
    const r2 = await request.get(`${API}/vista/longitudinal/summary`);
    expect(r2.status(), '/vista/longitudinal/summary not 500').not.toBe(500);
    const r3 = await request.get(`${API}/vista/longitudinal/summary?dfn=N`);
    expect(r3.status(), '/vista/longitudinal/summary?dfn=N not 500').not.toBe(500);
    const r4 = await request.get(`${API}/vista/longitudinal/timeline`);
    expect(r4.status(), '/vista/longitudinal/timeline not 500').not.toBe(500);
  });

  test('UI component files exist on disk', async () => {
    const componentNames = ['LongitudinalPanel.tsx'];
    function findFile(dir: string, name: string, depth = 0): boolean {
      if (depth > 6) return false;
      try {
        for (const entry of readdirSync(dir, { withFileTypes: true })) {
          if (entry.isFile() && entry.name === name) return true;
          if (entry.isDirectory() && !entry.name.startsWith('.') && entry.name !== 'node_modules') {
            if (findFile(join(dir, entry.name), name, depth + 1)) return true;
          }
        }
      } catch {
        /* permission error or similar */
      }
      return false;
    }
    let found = 0;
    for (const name of componentNames) {
      if (findFile(join(ROOT, 'apps'), name)) found++;
    }
    expect(found, `Expected >= 1 of 1 UI files for Phase 540`).toBeGreaterThan(0);
  });

  test('integration-pending responses are compliant', async ({ request }) => {
    // Spot-check first accessible route for pending compliance
    const res = await request.get(`${API}/vista/longitudinal/meds-summary`);
    expect(res.status(), '/vista/longitudinal/meds-summary should respond').toBeDefined();
    if (res.status() === 200) {
      const body = await res.json().catch(() => null);
      if (body && body.status === 'integration-pending') {
        expect(body, 'integration-pending must have nextSteps').toHaveProperty('nextSteps');
      }
    }
  });
});

test.describe('Phase 541: VA GUI Hybrids Capability Map (Wave 39, P11)', () => {
  test('API routes respond (no 500)', async ({ request }) => {
    const r0 = await request.get(`${API}/vista/hybrids/map`);
    expect(r0.status(), '/vista/hybrids/map not 500').not.toBe(500);
    const r1 = await request.get(`${API}/vista/hybrids/summary`);
    expect(r1.status(), '/vista/hybrids/summary not 500').not.toBe(500);
  });

  test('UI component files exist on disk', async () => {
    const componentNames = ['page.tsx'];
    function findFile(dir: string, name: string, depth = 0): boolean {
      if (depth > 6) return false;
      try {
        for (const entry of readdirSync(dir, { withFileTypes: true })) {
          if (entry.isFile() && entry.name === name) return true;
          if (entry.isDirectory() && !entry.name.startsWith('.') && entry.name !== 'node_modules') {
            if (findFile(join(dir, entry.name), name, depth + 1)) return true;
          }
        }
      } catch {
        /* permission error or similar */
      }
      return false;
    }
    let found = 0;
    for (const name of componentNames) {
      if (findFile(join(ROOT, 'apps'), name)) found++;
    }
    expect(found, `Expected >= 1 of 1 UI files for Phase 541`).toBeGreaterThan(0);
  });

  test('integration-pending responses are compliant', async ({ request }) => {
    // Spot-check first accessible route for pending compliance
    const res = await request.get(`${API}/vista/hybrids/map`);
    expect(res.status(), '/vista/hybrids/map should respond').toBeDefined();
    if (res.status() === 200) {
      const body = await res.json().catch(() => null);
      if (body && body.status === 'integration-pending') {
        expect(body, 'integration-pending must have nextSteps').toHaveProperty('nextSteps');
      }
    }
  });
});

test.describe('Phase 556: Frontend Config Hygiene — IMPLEMENT', () => {
  test('UI component files exist on disk', async () => {
    const componentNames = [
      'BrowserTerminal.tsx',
      'CPRSModals.tsx',
      'MessagingTasksPanel.tsx',
      'NotesPanel.tsx',
    ];
    function findFile(dir: string, name: string, depth = 0): boolean {
      if (depth > 6) return false;
      try {
        for (const entry of readdirSync(dir, { withFileTypes: true })) {
          if (entry.isFile() && entry.name === name) return true;
          if (entry.isDirectory() && !entry.name.startsWith('.') && entry.name !== 'node_modules') {
            if (findFile(join(dir, entry.name), name, depth + 1)) return true;
          }
        }
      } catch {
        /* permission error or similar */
      }
      return false;
    }
    let found = 0;
    for (const name of componentNames) {
      if (findFile(join(ROOT, 'apps'), name)) found++;
    }
    expect(found, `Expected >= 1 of 4 UI files for Phase 556`).toBeGreaterThan(0);
  });
});

test.describe('Phase 567: Consolidate Patient Model & Top Duplicates', () => {
  test('UI component files exist on disk', async () => {
    const componentNames = [
      'data-cache.tsx',
      'page.tsx',
      'patient-context.tsx',
      'session-context.tsx',
    ];
    function findFile(dir: string, name: string, depth = 0): boolean {
      if (depth > 6) return false;
      try {
        for (const entry of readdirSync(dir, { withFileTypes: true })) {
          if (entry.isFile() && entry.name === name) return true;
          if (entry.isDirectory() && !entry.name.startsWith('.') && entry.name !== 'node_modules') {
            if (findFile(join(dir, entry.name), name, depth + 1)) return true;
          }
        }
      } catch {
        /* permission error or similar */
      }
      return false;
    }
    let found = 0;
    for (const name of componentNames) {
      if (findFile(join(ROOT, 'apps'), name)) found++;
    }
    expect(found, `Expected >= 1 of 4 UI files for Phase 567`).toBeGreaterThan(0);
  });
});
