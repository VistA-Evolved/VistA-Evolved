# Phase 6B — Add Vitals (VERIFY — no changes)

Verify:

- POST /vista/vitals returns ok:true OR returns an honest VistA error with ok:false
- GET /vista/vitals reflects new entry (or count changes)
- UI refresh works
- no secrets committed

Output: PASS/FAIL + curl POST/GET commands.
