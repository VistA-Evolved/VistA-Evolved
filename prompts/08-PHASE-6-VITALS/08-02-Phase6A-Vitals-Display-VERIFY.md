# Phase 6A — Vitals Display (VERIFY — no changes)

Verify:

- /vista/vitals?dfn=1 returns ok:true
- results parse correctly as ien/type/value/datetime
- UI shows vitals list/table
- earlier endpoints still pass (/health, /vista/ping, /vista/patient-search)

Output: PASS/FAIL + curl commands.
