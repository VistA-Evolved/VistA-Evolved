# Phase 503 — Notes

- /health and /ready already exist in inline-routes.ts
- DegradedBanner already polls /ready in cprs/layout.tsx
- Circuit breaker already in rpc-resilience.ts
- This phase validates the pattern + adds admin force-open for simulation
