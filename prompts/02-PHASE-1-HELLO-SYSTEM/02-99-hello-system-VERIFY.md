# Phase 1 — Hello System (VERIFY — no changes)

Verify:

- pnpm -r install succeeds
- web dev server runs and homepage contains expected text
- api dev server runs and /health returns {"ok":true}
- CI workflow exists
- CodeQL workflow exists

Output: PASS/FAIL + commands.
