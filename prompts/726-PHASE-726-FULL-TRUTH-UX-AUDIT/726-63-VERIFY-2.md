# Phase 726-63 Verify - Order Sets Slice

## Verification Goal

Prove `/cprs/order-sets` against the live canonical stack, confirm whether it is a local quick-order surface or a live VistA-backed surface, and record only browser-backed truth.

## Verification Steps

1. Verify Docker, API, and VistA are reachable on the canonical VEHU stack.
2. Corroborate any similarly named live order-set routes so the browser page is not overstated as a VistA-backed feature when it is local-only.
3. Browser-prove `/cprs/order-sets` in an authenticated session, including adding one or more templates to draft orders and following the handoff to the Orders tab.
4. Browser-prove `/cprs/order-sets` in a fresh unauthenticated context and confirm the route behaves honestly.
5. If code changes were required, re-run the browser proof after the fix.
6. Record the slice in the browser audit artifact, runtime overrides, ops summary, and notion update.
7. Regenerate `pnpm audit:ui-estate:runtime` and `pnpm audit:ui-estate:truth` and confirm the checklist advances.

## Acceptance Criteria

1. The browser description of the page matches its real client-side and backend contract.
2. Any local-only behavior is labeled truthfully and not overstated as live VistA order-set functionality.
3. Any code change is minimal, evidence-backed, and re-proven in the browser.
4. The Phase 726 audit artifact and runtime checklist reflect the completed slice.