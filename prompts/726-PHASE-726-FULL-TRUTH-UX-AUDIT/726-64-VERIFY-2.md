# Phase 726-64 VERIFY - Remote Data Viewer Slice

## Scope

Verify the `/cprs/remote-data-viewer` slice against the canonical browser-controlled Phase 726 audit flow.

## Required checks

1. Corroborate:
   - `GET /vista/remote-facilities`
   - `GET /vista/remote-data?...`
   - `GET /admin/registry/default`
2. Browser-prove authenticated route behavior.
3. Browser-prove unauthenticated route behavior.
4. If patched, re-prove the corrected state.
5. Record evidence in:
   - `artifacts/phase726-p1-browser-control-audit.md`
   - `data/ui-estate/runtime-ui-audit-overrides.json`
   - `ops/summary.md`
   - `ops/notion-update.json`
6. Regenerate:
   - `pnpm audit:ui-estate:runtime`
   - `pnpm audit:ui-estate:truth`