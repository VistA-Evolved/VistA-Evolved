# Phase 465 — ED Board Adapter VERIFY

## Gates

1. ed-routes.ts registers as Fastify plugin
2. Endpoints: POST /ed/visits, GET /ed/visits, POST triage/bed/disposition
3. GET /ed/board returns metrics
4. GET /ed/beds returns bed list
