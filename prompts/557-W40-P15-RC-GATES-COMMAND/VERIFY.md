# Phase 557 — Verify — RC Gates Command

## Gates
1. `scripts/qa-rc.mjs` exists
2. `package.json` contains `"qa:rc"` script
3. `pnpm qa:rc` exits 0 with all 9 gates passing
4. `artifacts/qa-rc-evidence.json` written with correct structure
