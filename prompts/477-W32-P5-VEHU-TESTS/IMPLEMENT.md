# Phase 477 — W32-P5: VEHU Credentials + Test Harness Alignment

## Implementation Steps

1. Create `.env.vehu.example` with VEHU-specific env vars
2. Create `scripts/dev/use-vehu-env.ps1` helper script
3. Update `.env.example` with VEHU guidance comments
4. Document legacy vs VEHU credential conventions

## Files Touched

- `apps/api/.env.vehu.example` (created)
- `scripts/dev/use-vehu-env.ps1` (created)
- `apps/api/.env.example` (updated with VEHU guidance)
