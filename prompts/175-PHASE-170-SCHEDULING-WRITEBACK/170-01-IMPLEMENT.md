# Phase 170 — Scheduling Writeback Maturity

## Objective
Perfect request-only scheduling mode with truth gates enforced.

## Implementation Steps
1. Probe SDES RPC availability for writeback
2. Implement request-only mode with staff approval queue
3. Ensure UI never claims "scheduled" unless VistA confirms
