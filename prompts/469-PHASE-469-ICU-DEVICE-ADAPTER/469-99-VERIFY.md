# Phase 469 — ICU Device Adapter VERIFY

## Gates

1. icu-routes.ts registers as Fastify plugin
2. Endpoints: POST /icu/admissions, GET flowsheet, POST vent/io/scores
3. GET /icu/beds, GET /icu/metrics
4. Runbook documents all endpoints
