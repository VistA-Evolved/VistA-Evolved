# Phase 163 — VERIFY

## Gates

1. API TypeScript compiles clean
2. Web TypeScript compiles clean
3. Validation types export correctly
4. Dependency validator detects missing deps and circular refs
5. Boundary checker detects route pattern overlaps
6. Coverage validator checks capability-module alignment
7. Routes wired into server
8. Store-policy entry present
9. UI page renders without errors
10. Runbook exists
