# Phase 345 -- VERIFY

## Gates
1. `verify-wave16-security.ps1` exists and is non-empty
2. `security-cert-posture.ts` compiles clean
3. All W16 source files (P2-P8) exist
4. `verify-latest.ps1` delegates to wave16 verifier
5. tsc --noEmit passes
