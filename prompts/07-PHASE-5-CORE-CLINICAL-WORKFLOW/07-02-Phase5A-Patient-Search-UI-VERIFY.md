# Phase 5A — Patient Search UI (VERIFY — no changes)

Verify:
- apps/web dev server starts
- homepage links to /patient-search
- /patient-search loads
- default query SMI triggers fetch
- results appear (count >= 1)
- clicking a result displays Selected DFN and Selected Name
- no hydration warnings in console (use client present)

Output:
PASS/FAIL checklist + which file to fix if fail.
