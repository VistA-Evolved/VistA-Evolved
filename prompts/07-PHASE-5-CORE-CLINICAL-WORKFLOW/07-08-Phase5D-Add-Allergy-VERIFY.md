# Phase 5D — Add Allergy (VERIFY — no changes)

Verify:
1) POST /vista/allergies with TEST ALLERGY returns ok:true OR ok:false with -1 duplicate message (acceptable).
2) GET /vista/allergies shows updated list or increased count (if not duplicate).
3) UI form works and refreshes list.
4) No secrets committed.
5) verify-latest.ps1 still passes (Phase 1–4B or later).

Output:
PASS/FAIL + exact curl commands.
