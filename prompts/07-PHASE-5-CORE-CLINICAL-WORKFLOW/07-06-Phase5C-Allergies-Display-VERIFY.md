# Phase 5C — Allergies Display (VERIFY — no changes)

Verify:
- /vista/allergies?dfn=1 returns ok:true (count may be 0 but must not error)
- response results never contain empty id entries
- UI shows allergies section and list
- earlier endpoints still work

Output: PASS/FAIL + curl commands.
