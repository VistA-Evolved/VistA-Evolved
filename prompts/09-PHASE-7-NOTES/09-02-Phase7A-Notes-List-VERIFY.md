# Phase 7A — Notes List (VERIFY — no changes)

Verify:

- /vista/notes?dfn=1 returns ok:true
- UI displays notes list (even if empty)
- earlier endpoints still pass

Commands:
curl http://127.0.0.1:3001/vista/notes?dfn=1
