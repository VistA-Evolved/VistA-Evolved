## Verify

- [ ] Banner text matches `/ready` semantics
- [ ] Reachable VistA + open circuit breaker does not render as `VistA unreachable`
- [ ] `useSystemStatus()` still blocks writes when degraded/unreachable
- [ ] No new TypeScript errors in edited files