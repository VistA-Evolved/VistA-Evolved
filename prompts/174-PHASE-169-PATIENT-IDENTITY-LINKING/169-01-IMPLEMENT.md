# Phase 169 — Patient Identity Linking (Portal <-> VistA)

## Objective
Implement secure patient identity linking flow for portal -> VistA mapping.

## Implementation Steps
1. Implement identity verification + linking endpoints
2. Staff approval workflow (optional)
3. Tenant-scoped, audited, no PHI in logs
