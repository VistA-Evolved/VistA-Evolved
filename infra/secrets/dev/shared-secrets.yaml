# infra/secrets/dev/shared-secrets.yaml
# SOPS-encrypted values for the ve-shared chart (dev environment)
#
# To edit:   sops infra/secrets/dev/shared-secrets.yaml
# To create: sops --encrypt --in-place infra/secrets/dev/shared-secrets.yaml
#
# This file is committed encrypted. Decrypted values never touch disk
# outside of `sops --decrypt` or `helm secrets` plugin.

postgres:
  password: changeme-in-dev

keycloak:
  adminPassword: admin

minio:
  accessKey: minioadmin
  secretKey: minioadmin

# VistA sandbox defaults
vista:
  accessCode: PROV123
  verifyCode: 'PROV123!!'
