# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Build output
*.tsbuildinfo
dist/
build/
out/
coverage/

# Turborepo / Next / Vite / misc
.next/
.vite/
.cache/
.turbo/

# Docker
**/.docker/
docker-compose.override.yml

# Reference repos (large, observe-only)
/reference/
reference/cprs/

# Vendor upstream mirrors (large, observe-only -- keep LOCK.json)
vendor/worldvista/*/
!vendor/worldvista/LOCK.json
vendor/worldvista/sync.log

# Analytics local data (JSONL event persistence)
apps/api/data/

# Runtime logs (never commit)
/logs/
apps/api/logs/
apps/api/cookies*.txt
apps/api/verify*.json

# Root-level test / curl artifacts (never commit)
# Exclude all root-level JSON except known config files
/*.json
!package.json
!pnpm-workspace.yaml
!docker-compose.prod.yml
# Exclude all root-level txt except docs
/*.txt
/tmp_probe.m

# Playwright state + test results
pw-*.txt
verify-*.txt
apps/web/e2e/.auth/
e2e-results.json
e2e-report/
test-results/
test-results.json
playwright-report/

# RCM exports
data/rcm-exports/

# Evidence bundles (generated)
/artifacts/
docs/evidence/*/
data/evidence/

# Phase 53: Governance anti-sprawl rules
/reports/
docs/reports/
docs/verify/
**/*phase*-verify-report*.md
**/*verify-output*

# Phase 95B: Platform SQLite database
data/platform.db
data/platform.db-wal
data/platform.db-shm

# Phase 134: DR backup artifacts
/backups/
