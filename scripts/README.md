# scripts

Helper scripts for setup and automation.

## Examples (planned)

- Environment checks (Node, Docker, ports)
- Start/stop/reset local VistA stack (wrapping docker compose)
- Simple bridge test scripts (Node ↔ YottaDB)
- Data utilities (export/import test fixtures)

## Rule

Scripts should be safe to run repeatedly (idempotent where possible).
